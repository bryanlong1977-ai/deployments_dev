# Network Deployment 2

Deployment Step 8

## Description
This deployment step creates the following Azure resources as part of the Landing Zone infrastructure.

## Resources Created
See main.tf for resources.

## State Management
- **State File**: `hub-spoke-primary/connectivity/network-deployment-2.tfstate`
- **Example**: `hub-spoke-primary/connectivity/network-deployment-1.tfstate`
- **IMPORTANT**: This deployment has its own isolated state file. Do not share state with other deployments.
- State locking is enabled to prevent concurrent modifications.

## Dependencies
This deployment may reference outputs from previous deployments using `terraform_remote_state` data sources.
See `remote_state.tf` (if present) for dependency configurations.

## Deployment Instructions
1. Navigate to this directory
2. Initialize Terraform: `terraform init`
3. Review plan: `terraform plan -compact-warnings -var-file=../../config/terraform.tfvars`
4. Apply changes: `terraform apply -compact-warnings -var-file=../../config/terraform.tfvars`

> **Note**: All deployments share a single `terraform.tfvars` in the `config/` folder at the topology root.
> The `-var-file=../../config/terraform.tfvars` flag references that shared file.

Generated by CloudCraft AI - 2026-03-02T16:57:12.397388+00:00
